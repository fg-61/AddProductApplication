@model Product
@{
    ViewBag.Title = @Model.Name;
}
<div class="text-center col-lg-4">
    <form method="post" asp-controller="Product" asp-action="UpdateProduct">
        <div class="form-group">
            <p class="col-form-label" style="text-align:left;">Ürün Adı:</p>
            <input type="text" value=@Model.Name class="form-control" name="Name" id="product-name">
        </div>
        <div class="form-group">
            <p class="col-form-label" style="text-align:left;">Ürün Kodu:</p>
            <input type="text" value=@Model.Code class="form-control" name="Code" id="product-code">
        </div>
        <div class="form-group">
            <p class="col-form-label" style="text-align:left;">Fiyat:</p>
            <input type="number" min="0" value=@Model.UnitPrice.ToString().Replace(',','.') class="form-control" name="UnitPrice"  id="product-price">
        </div>
        <br>
        <div class="justify-content-center">
            <a class="btn btn-secondary" asp-controller="Product" asp-action="GetAllProduct">İptal Et</a>
            <button type="submit" id="save-product" class="btn btn-primary">Kaydet</button>
        </div>
    </form>
</div>